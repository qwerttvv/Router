--- a/control/bpf_stub.go
+++ b/control/bpf_stub.go
@@ -34,7 +34,7 @@ type bpfDaeParam struct {
 	Dae0peerMac     [6]uint8
 	PaddingAfterMac [2]uint8
 	UseRedirectPeer uint8 // 0=use bpf_redirect(), 1=use bpf_redirect_peer() when safe
-	Padding1        uint8
+	HasBtf          uint8
 	Padding2        uint16
 	DaeSocketMark   uint32 // mark set on dae's own sockets to identify them in eBPF
 }
--- a/control/bpf_utils.go
+++ b/control/bpf_utils.go
@@ -413,6 +413,11 @@ retryLoadBpf:
 		peerMac = [6]byte(hwAddr)
 	} // else: keep zero MAC for L3 netkit
 
+	hasBtf := uint8(0)
+	if _, err := os.Stat("/sys/kernel/btf/vmlinux"); err == nil {
+		hasBtf = 1
+	}
+
 	constants := map[string]interface{}{
 		"PARAM": struct {
 			tproxyPort      uint32
@@ -422,7 +427,7 @@ retryLoadBpf:
 			dae0peerMac     [6]byte
 			paddingAfterMac [2]uint8
 			useRedirectPeer uint8
-			padding1        uint8
+			hasBtf          uint8
 			padding2        uint16
 			daeSocketMark   uint32
 		}{
@@ -433,7 +438,7 @@ retryLoadBpf:
 			dae0peerMac:     peerMac,
 			paddingAfterMac: [2]uint8{0, 0},
 			useRedirectPeer: useRedirectPeer,
-			padding1:        0,
+			hasBtf:          hasBtf,
 			padding2:        0,
 			daeSocketMark:   soMarkFromDae,
 		},
--- a/control/kern/tproxy.c
+++ b/control/kern/tproxy.c
@@ -176,7 +176,7 @@ struct dae_param {
 	__u8 dae0peer_mac[6];
 	__u8 padding_after_mac[2]; // pad to align use_redirect_peer
 	__u8 use_redirect_peer;
-	__u8 padding1;
+	__u8 has_btf;
 	__u16 padding2;
 	// dae_socket_mark is set on dae's own sockets (Anyfrom pool) to identify them.
 	// When bpf_sk_lookup_* finds a socket, we check this mark to skip dae's own sockets.
@@ -3304,42 +3304,48 @@ static int __noinline get_real_comm_loop
 static __always_inline int get_pid_pname(struct pid_pname *pid_pname)
 {
 	int ret;
-	// Get pointer to args string.
-	struct task_struct *task = (void *)bpf_get_current_task();
-	char *args = (void *)BPF_CORE_READ(task, mm, arg_start);
-
-	// Read args to buffer.
-	char arg_buf[MAX_ARG_LEN]; // Allocate it out of ctx to pass CO-RE
-	struct get_real_comm_ctx ctx = {};
-
-	ctx.arg_buf = arg_buf;
-	ret = bpf_core_read_user_str(arg_buf, MAX_ARG_LEN, args);
-	if (unlikely(ret < 0)) {
-		bpf_printk(
-			"failed to read process name: bpf_core_read_user_str: %d",
-			ret);
-		return ret;
-	}
 
-	// Find range of command name.
-	ret = bpf_loop(MAX_ARG_LEN, get_real_comm_loop_cb, &ctx, 0);
-	if (unlikely(ret < 0))
-		return ret;
+	// Populate tgid
+	pid_pname->last_seen_ns = bpf_ktime_get_ns();
+	pid_pname->pid = bpf_get_current_pid_tgid() >> 32;
 
-	u8 offset = ctx.l;
+	if (PARAM.has_btf) {
+		// Get pointer to args string.
+		struct task_struct *task = (void *)bpf_get_current_task();
+		char *args = (void *)BPF_CORE_READ(task, mm, arg_start);
+
+		// Read args to buffer.
+		char arg_buf[MAX_ARG_LEN]; // Allocate it out of ctx to pass CO-RE
+		struct get_real_comm_ctx ctx = {};
+
+		ctx.arg_buf = arg_buf;
+		ret = bpf_core_read_user_str(arg_buf, MAX_ARG_LEN, args);
+		if (unlikely(ret < 0)) {
+			bpf_printk(
+				"failed to read process name: bpf_core_read_user_str: %d",
+				ret);
+			return ret;
+		}
 
-	for (u8 i = 0; i < TASK_COMM_LEN; i++) {
-		if (offset + i < MAX_ARG_LEN && arg_buf[offset + i] != '\0') {
-			pid_pname->pname[i] = arg_buf[offset + i];
-		} else {
-			pid_pname->pname[i] = '\0';
-			break;
+		// Find range of command name.
+		ret = bpf_loop(MAX_ARG_LEN, get_real_comm_loop_cb, &ctx, 0);
+		if (unlikely(ret < 0))
+			return ret;
+
+		u8 offset = ctx.l;
+
+		for (u8 i = 0; i < TASK_COMM_LEN; i++) {
+			if (offset + i < MAX_ARG_LEN && arg_buf[offset + i] != '\0') {
+				pid_pname->pname[i] = arg_buf[offset + i];
+			} else {
+				pid_pname->pname[i] = '\0';
+				break;
+			}
 		}
+	} else {
+		bpf_get_current_comm(pid_pname->pname, sizeof(pid_pname->pname));
 	}
 
-	// Populate tgid
-	pid_pname->last_seen_ns = bpf_ktime_get_ns();
-	pid_pname->pid = bpf_get_current_pid_tgid() >> 32;
 	return 0;
 }
 
