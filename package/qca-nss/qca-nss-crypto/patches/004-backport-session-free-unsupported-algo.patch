--- a/v2.0/src/nss_crypto_ctrl.c
+++ b/v2.0/src/nss_crypto_ctrl.c
@@ -988,6 +988,7 @@ int nss_crypto_session_alloc(struct nss_
 
 	if (!found) {
 		atomic_inc(&ctrl->cstats.alloc_fail_noalgo);
+		kref_put(&ctx->ref, nss_crypto_ctx_free);
 		return -ENOENT;
 	}
 
