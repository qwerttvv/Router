--- a/ecm_classifier_default.c
+++ b/ecm_classifier_default.c
@@ -32,7 +32,7 @@
 #include <net/route.h>
 #include <net/ip.h>
 #include <net/tcp.h>
-#include <asm/unaligned.h>
+#include <linux/unaligned.h>
 #include <asm/uaccess.h>	/* for put_user */
 #include <net/ipv6.h>
 #include <linux/inet.h>
--- a/ecm_classifier_dscp.c
+++ b/ecm_classifier_dscp.c
@@ -31,7 +31,7 @@
 #include <net/route.h>
 #include <net/ip.h>
 #include <net/tcp.h>
-#include <asm/unaligned.h>
+#include <linux/unaligned.h>
 #include <asm/uaccess.h>	/* for put_user */
 #include <net/ipv6.h>
 #include <linux/inet.h>
--- a/ecm_classifier_hyfi.c
+++ b/ecm_classifier_hyfi.c
@@ -31,7 +31,7 @@
 #include <net/route.h>
 #include <net/ip.h>
 #include <net/tcp.h>
-#include <asm/unaligned.h>
+#include <linux/unaligned.h>
 #include <asm/uaccess.h>	/* for put_user */
 #include <net/ipv6.h>
 #include <linux/inet.h>
@@ -437,7 +437,7 @@ static void ecm_classifier_hyfi_get_brid
 			struct net_device *master;
 			master = ecm_interface_get_and_hold_dev_master(dev);
 			if (master) {
-				strlcpy(name_buffer, master->name, IFNAMSIZ);
+				strscpy(name_buffer, master->name, IFNAMSIZ);
 				dev_put(master);
 				dev_put(dev);
 				break;
@@ -841,13 +841,13 @@ struct ecm_classifier_hyfi_instance *ecm
 		/* one of the bridge name is null, typical
 		 * routed connection. Consider valid bridge*/
 		if (strlen(to_bridge)) {
-			strlcpy(chfi->bridge_name, to_bridge, IFNAMSIZ);
+			strscpy(chfi->bridge_name, to_bridge, IFNAMSIZ);
 		} else if (strlen(from_bridge)) {
-			strlcpy(chfi->bridge_name, from_bridge, IFNAMSIZ);
+			strscpy(chfi->bridge_name, from_bridge, IFNAMSIZ);
 		}
 	} else if (!strncmp(to_bridge, from_bridge, IFNAMSIZ)) {
 		/* Pure bridge connection. Consider any one bridge */
-		strlcpy(chfi->bridge_name, to_bridge, IFNAMSIZ);
+		strscpy(chfi->bridge_name, to_bridge, IFNAMSIZ);
 	} else {
 		/* multi-bridge connection */
 		chfi->multi_bridge_flow = true;
--- a/ecm_classifier_mscs.c
+++ b/ecm_classifier_mscs.c
@@ -27,7 +27,7 @@
 #include <net/route.h>
 #include <net/ip.h>
 #include <net/tcp.h>
-#include <asm/unaligned.h>
+#include <linux/unaligned.h>
 #include <asm/uaccess.h>	/* for put_user */
 #include <net/ipv6.h>
 #include <linux/inet.h>
@@ -429,7 +429,6 @@ static void ecm_classifier_mscs_process(
 	struct ecm_db_connection_instance *ci = NULL;
 	struct ecm_front_end_connection_instance *feci;
 	ecm_front_end_acceleration_mode_t accel_mode;
-	int protocol;
 	uint32_t became_relevant = 0;
 	ecm_classifier_mscs_process_callback_t cb = NULL;
 	bool scs_result = false;
@@ -441,10 +440,10 @@ static void ecm_classifier_mscs_process(
 	struct net_device *src_dev = NULL;
 	struct net_device *dest_dev = NULL;
 	uint64_t slow_pkts;
+	struct ecm_classifier_mscs_get_priority_info get_priority_info = {0};
 #ifdef ECM_CLASSIFIER_MSCS_SCS_ENABLE
 	struct sp_rule_input_params flow_input_params;
 	struct sp_rule_output_params flow_output_params;
-	struct ecm_classifier_mscs_get_priority_info get_priority_info = {0};
 	struct ecm_classifier_mscs_rule_match_info rule_match_info = {0};
 	ecm_classifier_mscs_scs_priority_callback_t scs_cb = NULL;
 #endif
--- a/ecm_classifier_nl.c
+++ b/ecm_classifier_nl.c
@@ -31,7 +31,7 @@
 #include <net/route.h>
 #include <net/ip.h>
 #include <net/tcp.h>
-#include <asm/unaligned.h>
+#include <linux/unaligned.h>
 #include <asm/uaccess.h>	/* for put_user */
 #include <net/ipv6.h>
 #include <linux/inet.h>
--- a/ecm_classifier_pcc.c
+++ b/ecm_classifier_pcc.c
@@ -39,7 +39,7 @@
 #include <net/route.h>
 #include <net/ip.h>
 #include <net/tcp.h>
-#include <asm/unaligned.h>
+#include <linux/unaligned.h>
 #include <asm/uaccess.h>	/* for put_user */
 #include <net/ipv6.h>
 #include <linux/inet.h>
--- a/ecm_interface.c
+++ b/ecm_interface.c
@@ -35,7 +35,7 @@
 #include <net/ip.h>
 #include <net/tcp.h>
 #include <net/addrconf.h>
-#include <asm/unaligned.h>
+#include <linux/unaligned.h>
 #include <asm/uaccess.h>	/* for put_user */
 #include <linux/inet.h>
 #include <linux/in6.h>
@@ -1395,7 +1395,7 @@ __be32 ecm_interface_vxlan_gpe_get_vni_r
 		__be32 daddr;
 
 		daddr = netif_is_vxlan(indev) ? ip_hdr(skb)->saddr : ip_hdr(skb)->daddr;
-		rt = ip_route_output(&init_net, daddr, 0, 0, 0);
+		rt = ip_route_output(&init_net, daddr, 0, 0, 0, RT_SCOPE_UNIVERSE);
 		if (IS_ERR_OR_NULL(rt)) {
 			DEBUG_WARN("%px: VXLAN-GPE failed to get IPv4 route to: %pI4\n", dev, &daddr);
 			goto rt_error;
@@ -1718,7 +1718,7 @@ struct neighbour *ecm_interface_ipv4_nei
 	__be32 ipv4_addr;
 
 	ECM_IP_ADDR_TO_NIN4_ADDR(ipv4_addr, addr);
-	rt = ip_route_output(&init_net, ipv4_addr, 0, 0, 0);
+	rt = ip_route_output(&init_net, ipv4_addr, 0, 0, 0, RT_SCOPE_UNIVERSE);
 	if (IS_ERR_OR_NULL(rt)) {
 		return NULL;
 	}
@@ -1774,6 +1774,7 @@ struct neighbour *ecm_interface_ipv6_nei
  */
 bool ecm_interface_is_pptp(struct sk_buff *skb, const struct net_device *out)
 {
+#ifdef ECM_INTERFACE_PPTP_ENABLE
 	struct net_device *in;
 
 	/*
@@ -1798,6 +1799,7 @@ bool ecm_interface_is_pptp(struct sk_buf
 	}
 
 	dev_put(in);
+#endif
 	return false;
 }
 
@@ -1810,6 +1812,7 @@ bool ecm_interface_is_pptp(struct sk_buf
  */
 bool ecm_interface_is_l2tp_packet_by_version(struct sk_buff *skb, const struct net_device *out, int ver)
 {
+#ifdef ECM_INTERFACE_L2TPV2_PPTP_ENABLE
 	uint32_t flag = 0;
 	struct net_device *in;
 
@@ -1842,6 +1845,7 @@ bool ecm_interface_is_l2tp_packet_by_ver
 	}
 
 	dev_put(in);
+#endif
 	return false;
 }
 
@@ -1854,6 +1858,7 @@ bool ecm_interface_is_l2tp_packet_by_ver
  */
 bool ecm_interface_is_l2tp_pptp(struct sk_buff *skb, const struct net_device *out)
 {
+#ifdef ECM_INTERFACE_L2TPV2_PPTP_ENABLE
 	struct net_device *in;
 
 	/*
@@ -1876,6 +1881,7 @@ bool ecm_interface_is_l2tp_pptp(struct s
 	}
 
 	dev_put(in);
+#endif
 	return false;
 }
 
@@ -4174,7 +4180,7 @@ identifier_update:
 	 * PPP - but what is the channel type?
 	 * First: If this is multi-link then we do not support it
 	 */
-	if (ppp_is_multilink(dev) > 0) {
+	if (__ppp_is_multilink(dev) > 0) {
 		DEBUG_TRACE("%px: Net device: %px is MULTILINK PPP - Unknown to the ECM\n", feci, dev);
 		type_info.unknown.os_specific_ident = dev_interface_num;
 
@@ -4482,7 +4488,7 @@ static uint32_t ecm_interface_multicast_
 				 * For MLO bond netdevice, destination for multicast is bond netdevice itself
 				 * Therefore, slave lookup is not needed.
 				 */
-				if (ecm_front_end_is_lag_master(dest_dev) && !bond_is_mlo_device(dest_dev)) {
+				if (ecm_front_end_is_lag_master(dest_dev)) {
 					/*
 					 * Link aggregation
 					 * Figure out which slave device of the link aggregation will be used to reach the destination.
@@ -7741,6 +7747,7 @@ static void ecm_interface_regenerate_con
 		return;
 	}
 
+#ifdef ECM_DB_XREF_ENABLE
 	for (dir = 0; dir < ECM_DB_OBJ_DIR_MAX; dir++) {
 		/*
 		 * Re-generate all connections associated with this interface
@@ -7756,6 +7763,7 @@ static void ecm_interface_regenerate_con
 			ci[dir] = cin;
 		}
 	}
+#endif
 
 #ifdef ECM_MULTICAST_ENABLE
 	/*
@@ -8848,6 +8856,25 @@ int ecm_interface_resolve_nl80211_family
 	 */
 	nlh = (struct nlmsghdr *)buf;
 	while (NLMSG_OK(nlh, size)) {
+		if (nlh->nlmsg_type == NLMSG_ERROR) {
+			struct nlmsgerr *nlerr = nlmsg_data(nlh);
+
+			if (nlmsg_len(nlh) < sizeof(*nlerr)) {
+				DEBUG_WARN("%px: Invalid NL error message length\n", nlh);
+				kfree(buf);
+				return -EINVAL;
+			}
+
+			if (nlerr->error) {
+				ret = nlerr->error;
+				kfree(buf);
+				return ret;
+			}
+
+			nlh = NLMSG_NEXT(nlh, size);
+			continue;
+		}
+
 		DEBUG_INFO("%px: Received an NL response, length %d type %d\n", nlh, nlh->nlmsg_len, nlh->nlmsg_type);
 		mc_group_id = ecm_interface_parse_genl_ctrl_response(nlh);
 		if (mc_group_id < 0) {
@@ -8868,16 +8895,19 @@ int ecm_interface_resolve_nl80211_family
 	 * Add this socket as a memeber to the MLME multicast group of the
 	 * nl80211 family.
 	 */
-	if (mc_group_id != ECM_INTERFACE_NL80211_MC_GROUP_INVALID_ID) {
-		ret = sock->ops->setsockopt(sock, SOL_NETLINK, NETLINK_ADD_MEMBERSHIP, KERNEL_SOCKPTR((void *)&mc_group_id), sizeof(mc_group_id));
-		if (ret < 0) {
-			DEBUG_WARN("%px: Failed to set the multicast membership %d ret %d\n", sock, mc_group_id, ret);
-			return ret;
-		}
+	if (mc_group_id == ECM_INTERFACE_NL80211_MC_GROUP_INVALID_ID) {
+		DEBUG_WARN("%px: Failed to resolve the nl80211 MLME multicast group\n", sock);
+		return -ENOENT;
+	}
 
-		DEBUG_INFO("%px: Adding a this sokcet as a member to NL80211 MLME multicast group %d \n", sock, mc_group_id);
+	ret = sock->ops->setsockopt(sock, SOL_NETLINK, NETLINK_ADD_MEMBERSHIP, KERNEL_SOCKPTR((void *)&mc_group_id), sizeof(mc_group_id));
+	if (ret < 0) {
+		DEBUG_WARN("%px: Failed to set the multicast membership %d ret %d\n", sock, mc_group_id, ret);
+		return ret;
 	}
 
+	DEBUG_INFO("%px: Adding a this sokcet as a member to NL80211 MLME multicast group %d \n", sock, mc_group_id);
+
 	return 0;
 }
 
@@ -8919,7 +8949,11 @@ static void ecm_interface_wifi_event_thr
 	 */
 	err = ecm_interface_resolve_nl80211_family(__ewn.sock, &saddr);
 	if (err < 0) {
-		DEBUG_ERROR("Failed to resolve the nl80211 generic netlink family err %d\n", err);
+		if ((err == -ENOENT) || (err == -EINVAL)) {
+			pr_notice("qca-nss-ecm: no Wi-Fi/nl80211 detected; disabling ECM WiFi event listener\n");
+		} else {
+			pr_err("qca-nss-ecm: failed to resolve the nl80211 generic netlink family err %d\n", err);
+		}
 		goto exit2;
 	}
 
@@ -8975,7 +9009,7 @@ static int ecm_interface_wifi_event_iwev
 		/*
 		 * Copy the base data structure to get iwe->len
 		 */
-		memcpy(&iwe_buf, pos, IW_EV_LCP_LEN);
+		memcpy(&iwe_buf, pos, min_t(size_t, IW_EV_LCP_LEN, (size_t)(end - pos)));
 
 		/*
 		 * Check that len is valid and that we have that much in the buffer.
@@ -8992,10 +9026,10 @@ static int ecm_interface_wifi_event_iwev
 			dpos = (char *)&iwe_buf.u.data.length;
 			dlen = dpos - (char *)&iwe_buf;
 
-			memcpy(dpos, pos + IW_EV_LCP_LEN, sizeof(struct iw_event) - dlen);
+			memcpy(dpos, pos + IW_EV_LCP_LEN, min_t(size_t, sizeof(struct iw_event) - dlen, (size_t)(end - pos - IW_EV_LCP_LEN)));
 
 			if (custom + iwe->u.data.length > end) {
-				DEBUG_WARN("Invalid buffer length received in the event iwe->u.data.length %d\n", iwe->u.data.length);
+				DEBUG_WARN("Invalid buffer length received in the event iwe->u.data.length %d\n", (int)iwe->u.data.length);
 				return -1;
 			}
 
@@ -9003,7 +9037,7 @@ static int ecm_interface_wifi_event_iwev
 			 * Check the flags of iw event if it indicates the IW authorized signal.
 			 */
 			if (iwe->u.data.flags == ECM_INTERFACE_WIFI_EVENT_NODE_AUTH) {
-				dbuf = kzalloc((iwe->u.data.length + 1), GFP_KERNEL);
+				dbuf = kzalloc(iwe->u.data.length, GFP_KERNEL);
 				if (!dbuf) {
 					DEBUG_WARN("Failed to allocated a buffer to process the custom event");
 					return -1;
@@ -9026,16 +9060,16 @@ static int ecm_interface_wifi_event_iwev
 			return 0;
 		}
 
-		if ((iwe->len > sizeof(struct iw_event)) || (iwe->len + pos) > end) {
+		if ((iwe->len > sizeof(struct iw_event)) || (iwe->len + pos > end)) {
 			return -1;
 		}
 
 		/*
 		 * Do the copy again with the full length.
 		 */
-		memcpy(&iwe_buf, pos, iwe->len);
+		memcpy(&iwe_buf, pos, min_t(size_t, iwe->len, (size_t)(end - pos)));
 
-		if (iwe->cmd == IWEVEXPIRED) {
+		if (iwe->cmd == IWEVEXPIRED && iwe->len >= sizeof(struct iw_event)) {
 			DEBUG_INFO("STA %pM leaving\n", (uint8_t *)iwe->u.addr.sa_data);
 			ecm_interface_node_connections_defunct((uint8_t *)iwe->u.addr.sa_data, ECM_DB_IP_VERSION_IGNORE);
 		} else {
@@ -9205,7 +9239,7 @@ int ecm_interface_wifi_event_stop(void)
  * ecm_interface_igs_enabled_handler()
  *	IGS enabled check sysctl node handler.
  */
-static int ecm_interface_igs_enabled_handler(struct ctl_table *ctl, int write, void __user *buffer,
+static int ecm_interface_igs_enabled_handler(const struct ctl_table *ctl, int write, void __user *buffer,
 		 size_t *lenp, loff_t *ppos)
 {
 	int ret;
@@ -9246,7 +9280,7 @@ static int ecm_interface_igs_enabled_han
  * ecm_interface_src_check_handler()
  *	Source interface check sysctl node handler.
  */
-static int ecm_interface_src_check_handler(struct ctl_table *ctl, int write, void __user *buffer, size_t *lenp, loff_t *ppos)
+static int ecm_interface_src_check_handler(const struct ctl_table *ctl, int write, void __user *buffer, size_t *lenp, loff_t *ppos)
 {
 	int ret;
 	int current_value;
@@ -9287,7 +9321,7 @@ static int ecm_interface_src_check_handl
  * ecm_interface_src_check_no_flush_handler()
  *	Source interface check no flush sysctl node handler.
  */
-static int ecm_interface_src_check_no_flush_handler(struct ctl_table *ctl, int write, void __user *buffer, size_t *lenp, loff_t *ppos)
+static int ecm_interface_src_check_no_flush_handler(const struct ctl_table *ctl, int write, void __user *buffer, size_t *lenp, loff_t *ppos)
 {
 	int ret;
 
@@ -9311,7 +9345,7 @@ static int ecm_interface_src_check_no_fl
  * ecm_interface_mwan3_enable_handler()
  *	 mwan3 enable check sysctl node handler.
  */
-static int ecm_interface_mwan3_enable_handler(struct ctl_table *ctl, int write, void __user *buffer,
+static int ecm_interface_mwan3_enable_handler(const struct ctl_table *ctl, int write, void __user *buffer,
 		 size_t *lenp, loff_t *ppos)
 {
 	int ret;
@@ -9649,7 +9683,7 @@ static int ecm_interface_defunct_by_mac_
  * ecm_interface_defunct_by_mac_addr_handler()
  * 	Proc handler function for defunct the ecm rules by mac address
  */
-static int ecm_interface_defunct_by_mac_address_handler(struct ctl_table *ctl, int write, void *buffer, size_t *lenp, loff_t *ppos)
+static int ecm_interface_defunct_by_mac_address_handler(const struct ctl_table *ctl, int write, void *buffer, size_t *lenp, loff_t *ppos)
 {
 	/*
 	 * To mark a MAC address as defunct:
@@ -9707,7 +9741,6 @@ static struct ctl_table ecm_interface_ta
 		.mode			= 0644,
 		.proc_handler		= &ecm_interface_mwan3_enable_handler,
 	},
-	{ }
 };
 
 #ifdef ECM_INTERFACE_IPSEC_GLUE_LAYER_SUPPORT_ENABLE
@@ -10230,7 +10263,9 @@ int ecm_interface_init(void)
 #ifdef ECM_INTERFACE_OVS_BRIDGE_ENABLE
 	ovsmgr_notifier_register(&ecm_interface_ovs_notifier);
 #endif
+#ifdef ECM_INTERFACE_WIFI_EVENTS_ENABLE
 	ecm_interface_wifi_event_start();
+#endif
 
 	return 0;
 }
@@ -10262,7 +10297,9 @@ void ecm_interface_exit(void)
 		br_fdb_unregister_notify(&ecm_interface_node_br_fdb_delete_nb);
 	}
 #endif
+#ifdef ECM_INTERFACE_WIFI_EVENTS_ENABLE
 	ecm_interface_wifi_event_stop();
+#endif
 
 #ifdef ECM_INTERFACE_OVS_BRIDGE_ENABLE
 	ovsmgr_notifier_unregister(&ecm_interface_ovs_notifier);
