--- a/dtls/v1.0/nss_connmgr_dtls_netdev.c
+++ b/dtls/v1.0/nss_connmgr_dtls_netdev.c
@@ -160,9 +160,9 @@ static void nss_dtlsmgr_dev_setup(struct
 	dev->ethtool_ops = NULL;
 	dev->header_ops = NULL;
 	dev->netdev_ops = &nss_dtlsmgr_session_ops;
-	dev->destructor = NULL;
+	dev->priv_destructor = NULL;
 
-	memcpy(dev->dev_addr, "\xaa\xbb\xcc\xdd\xee\xff", dev->addr_len);
+	eth_hw_addr_set(dev, (const u8 *)"\xaa\xbb\xcc\xdd\xee\xff");
 	memset(dev->broadcast, 0xff, dev->addr_len);
 	memcpy(dev->perm_addr, dev->dev_addr, dev->addr_len);
 }
--- a/dtls/v2.0/nss_dtlsmgr_ctx.c
+++ b/dtls/v2.0/nss_dtlsmgr_ctx.c
@@ -19,6 +19,7 @@
  *	NSS DTLS Manager Context
  */
 
+#include <linux/vmalloc.h>
 #include <linux/version.h>
 #include <linux/types.h>
 #include <linux/ip.h>
--- a/dtls/v2.0/nss_dtlsmgr_ctx_dev.c
+++ b/dtls/v2.0/nss_dtlsmgr_ctx_dev.c
@@ -526,7 +526,7 @@ void nss_dtlsmgr_ctx_dev_setup(struct ne
 #else
 	dev->priv_destructor = nss_dtlsmgr_ctx_dev_free;
 #endif
-	memcpy(dev->dev_addr, "\xaa\xbb\xcc\xdd\xee\xff", dev->addr_len);
+	eth_hw_addr_set(dev, (const u8 *)"\xaa\xbb\xcc\xdd\xee\xff");
 	memset(dev->broadcast, 0xff, dev->addr_len);
 	memcpy(dev->perm_addr, dev->dev_addr, dev->addr_len);
 }
--- a/dtls/v2.0/nss_dtlsmgr_ctx_stats.c
+++ b/dtls/v2.0/nss_dtlsmgr_ctx_stats.c
@@ -25,6 +25,7 @@
 #include <linux/module.h>
 #include <linux/rtnetlink.h>
 #include <linux/types.h>
+#include <linux/vmalloc.h>
 #include <linux/version.h>
 
 #include <nss_api_if.h>
--- a/ipsecmgr/v2.0/nss_ipsecmgr_tunnel.c
+++ b/ipsecmgr/v2.0/nss_ipsecmgr_tunnel.c
@@ -445,7 +445,7 @@ static void nss_ipsecmgr_tunnel_setup(st
 	/*
 	 * Get the MAC address from the ethernet device
 	 */
-	random_ether_addr(dev->dev_addr);
+	eth_hw_addr_random(dev);
 
 	memset(dev->broadcast, 0xff, dev->addr_len);
 	memcpy(dev->perm_addr, dev->dev_addr, dev->addr_len);
--- a/ipsecmgr/v2.0/plugins/xfrm/nss_ipsec_xfrm.c
+++ b/ipsecmgr/v2.0/plugins/xfrm/nss_ipsec_xfrm.c
@@ -31,6 +31,7 @@
 #include <net/addrconf.h>
 #include <linux/netfilter.h>
 #include <linux/debugfs.h>
+#include <linux/vmalloc.h>
 
 #include <nss_api_if.h>
 #include <nss_ipsec.h>
--- a/openvpn/src/nss_ovpnmgr_crypto.c
+++ b/openvpn/src/nss_ovpnmgr_crypto.c
@@ -18,6 +18,7 @@
  * nss_ovpnmgr_crypto.c
  */
 #include <linux/version.h>
+#include <linux/vmalloc.h>
 #include <linux/types.h>
 #include <linux/module.h>
 #include <linux/if.h>
--- a/tls/nss_tlsmgr.c
+++ b/tls/nss_tlsmgr.c
@@ -22,6 +22,7 @@
  */
 
 #include <linux/version.h>
+#include <linux/vmalloc.h>
 #include <linux/types.h>
 #include <linux/module.h>
 #include <linux/if_arp.h>
--- a/tls/nss_tlsmgr_crypto.c
+++ b/tls/nss_tlsmgr_crypto.c
@@ -22,6 +22,7 @@
  */
 
 #include <linux/version.h>
+#include <linux/vmalloc.h>
 #include <linux/types.h>
 #include <linux/ip.h>
 #include <linux/tcp.h>
--- a/tls/nss_tlsmgr_ctx.c
+++ b/tls/nss_tlsmgr_ctx.c
@@ -22,6 +22,8 @@
  */
 
 #include <linux/version.h>
+#include <linux/timer.h>
+#include <linux/vmalloc.h>
 #include <linux/types.h>
 #include <linux/ip.h>
 #include <linux/tcp.h>
--- a/tls/nss_tlsmgr_tun.c
+++ b/tls/nss_tlsmgr_tun.c
@@ -22,6 +22,7 @@
  */
 
 #include <linux/version.h>
+#include <linux/timer.h>
 #include <linux/types.h>
 #include <linux/ip.h>
 #include <linux/tcp.h>
@@ -179,7 +180,7 @@ static void nss_tlsmgr_tun_setup(struct
 	/*
 	 * Get the MAC address from the ethernet device
 	 */
-	random_ether_addr(dev->dev_addr);
+	eth_hw_addr_random(dev);
 
 	memset(dev->broadcast, 0xff, dev->addr_len);
 	memcpy(dev->perm_addr, dev->dev_addr, dev->addr_len);
@@ -225,7 +226,7 @@ static void nss_tlsmgr_notify_event(stru
 #if (LINUX_VERSION_CODE < KERNEL_VERSION(4, 15, 0))
 	struct nss_tlsmgr_tun *tun = (struct nss_tlsmgr_tun *)data;
 #else
-	struct nss_tlsmgr_tun *tun = from_timer(tun, tm, notify.timer);
+	struct nss_tlsmgr_tun *tun = timer_container_of(tun, tm, notify.timer);
 #endif
 	nss_tlsmgr_notify_callback_t cb;
 	struct nss_tlsmgr_stats stats;
@@ -257,7 +258,7 @@ static void nss_tlsmgr_notify_decongesti
 #if (LINUX_VERSION_CODE < KERNEL_VERSION(4, 15, 0))
 	struct nss_tlsmgr_tun *tun = (struct nss_tlsmgr_tun *)data;
 #else
-	struct nss_tlsmgr_tun *tun = from_timer(tun, tm, notify.timer);
+	struct nss_tlsmgr_tun *tun = timer_container_of(tun, tm, decongest.timer);
 #endif
 	nss_tlsmgr_decongest_callback_t cb;
 	void *app_data;
@@ -325,7 +326,7 @@ void nss_tlsmgr_unregister_notify(struct
 	write_unlock_bh(&tun->lock);
 
 	BUG_ON(in_atomic());
-	del_timer_sync(&tun->notify.timer);
+	timer_delete_sync(&tun->notify.timer);
 }
 
 /*
@@ -452,7 +453,7 @@ void nss_tlsmgr_tun_del(struct net_devic
 
 	nss_tlsmgr_unregister_notify(dev);
 
-	del_timer_sync(&tun->decongest.timer);
+	timer_delete_sync(&tun->decongest.timer);
 	tun->decongest.app_data = NULL;
 	tun->decongest.cb = NULL;
 	tun->decongest.ticks = 0;
--- a/tls/nss_tlsmgr_tun.h
+++ b/tls/nss_tlsmgr_tun.h
@@ -23,6 +23,8 @@
 #ifndef __NSS_TLSMGR_TUN_H_
 #define __NSS_TLSMGR_TUN_H_
 
+#include <linux/timer.h>
+
 #define NSS_TLSMGR_MAX_HDR_SZ \
 	(sizeof(struct ipv6hdr) + sizeof(struct tcphdr) + sizeof(struct tlshdr) + AES_BLOCK_SIZE)
 #define NSS_TLSMGR_MAX_TRAILER_SZ (AES_BLOCK_SIZE + SHA256_DIGEST_SIZE + 1)
