--- a/nss_dp_main.c
+++ b/nss_dp_main.c
@@ -1091,6 +1091,7 @@ static int32_t nss_dp_probe(struct platf
 				&nss_dp_adjust_link,
 				dp_priv->phy_mii_type);
 		if (IS_ERR(dp_priv->phydev)) {
+			dp_priv->phydev = NULL;
 			netdev_dbg(netdev, "failed to connect to phy device\n");
 			goto phy_setup_fail;
 		}
