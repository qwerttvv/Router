--- a/Makefile
+++ b/Makefile
@@ -4,6 +4,8 @@
 
 obj-m += qca-nss-drv.o
 
+ccflags-y += $(NSS_DRV_EXTRA_INCLUDES)
+
 #
 # Regular NSS path
 #
@@ -26,7 +28,6 @@ qca-nss-drv-objs := \
 			nss_n2h_stats.o \
 			nss_n2h_strings.o \
 			nss_pm.o \
-			nss_profiler.o \
 			nss_project.o \
 			nss_rps.o \
 			nss_stats.o \
@@ -40,7 +41,6 @@ qca-nss-drv-objs += nss_hal/nss_hal.o
 
 ifneq "$(NSS_DRV_POINT_OFFLOAD)" "y"
 qca-nss-drv-objs += \
-			nss_gmac_stats.o \
 			nss_if.o \
 			nss_if_log.o \
 			nss_phys_if.o \
@@ -124,12 +124,14 @@ qca-nss-drv-objs += \
 endif
 endif
 
+ifeq ($(SoC),$(filter $(SoC),ipq806x))
 ifneq "$(NSS_DRV_TSTAMP_ENABLE)" "n"
 ccflags-y += -DNSS_DRV_TSTAMP_ENABLE
 qca-nss-drv-objs += \
 			 nss_tstamp.o \
 			 nss_tstamp_stats.o
 endif
+endif
 
 ifneq "$(NSS_DRV_GRE_ENABLE)" "n"
 ccflags-y += -DNSS_DRV_GRE_ENABLE
@@ -257,6 +259,7 @@ qca-nss-drv-objs += \
 			 nss_rmnet_rx_stats.o
 endif
 
+ifeq ($(SoC),$(filter $(SoC),ipq806x))
 ifneq "$(NSS_DRV_PORTID_ENABLE)" "n"
 ccflags-y += -DNSS_DRV_PORTID_ENABLE
 qca-nss-drv-objs += \
@@ -264,6 +267,7 @@ qca-nss-drv-objs += \
 			 nss_portid_log.o \
 			 nss_portid_stats.o
 endif
+endif
 
 ifneq "$(NSS_DRV_IGS_ENABLE)" "n"
 ccflags-y += -DNSS_DRV_IGS_ENABLE
@@ -272,12 +276,14 @@ qca-nss-drv-objs += \
 			 nss_igs_stats.o
 endif
 
+ifeq ($(SoC),$(filter $(SoC),ipq806x))
 ifneq "$(NSS_DRV_OAM_ENABLE)" "n"
 ccflags-y += -DNSS_DRV_OAM_ENABLE
 qca-nss-drv-objs += \
 			 nss_oam.o \
 			 nss_oam_log.o
 endif
+endif
 
 ifneq "$(NSS_DRV_CLMAP_ENABLE)" "n"
 ccflags-y += -DNSS_DRV_CLMAP_ENABLE
@@ -352,10 +358,14 @@ endif
 
 ifneq "$(NSS_DRV_WIFIOFFLOAD_ENABLE)" "n"
 ccflags-y += -DNSS_DRV_WIFIOFFLOAD_ENABLE
+ifeq ($(SoC),$(filter $(SoC),ipq806x))
 qca-nss-drv-objs += \
 			nss_wifi.o \
 			nss_wifi_log.o \
-			nss_wifi_stats.o \
+			nss_wifi_stats.o
+ccflags-y += -DNSS_HAL_IPQ806x_SUPPORT
+else
+qca-nss-drv-objs += \
 			nss_wifi_vdev.o \
 			nss_wifili.o \
 			nss_wifili_log.o \
@@ -363,6 +373,7 @@ qca-nss-drv-objs += \
 			nss_wifili_strings.o \
 			nss_wifi_mac_db.o
 endif
+endif
 
 ifneq "$(NSS_DRV_VLAN_ENABLE)" "n"
 ccflags-y += -DNSS_DRV_VLAN_ENABLE
@@ -381,7 +392,9 @@ endif
 
 ifeq ($(SoC),$(filter $(SoC),ipq806x))
 qca-nss-drv-objs += nss_data_plane/nss_data_plane_gmac.o \
-		    nss_hal/ipq806x/nss_hal_pvt.o
+		    nss_hal/ipq806x/nss_hal_pvt.o \
+		    nss_gmac_stats.o \
+		    nss_profiler.o
 
 ifneq "$(NSS_DRV_C2C_ENABLE)" "n"
 ccflags-y += -DNSS_DRV_C2C_ENABLE
--- a/exports/nss_bridge.h
+++ b/exports/nss_bridge.h
@@ -273,7 +273,7 @@ nss_tx_status_t nss_bridge_tx_set_mtu_ms
  * @return
  * Status of the Tx operation.
  */
-nss_tx_status_t nss_bridge_tx_set_mac_addr_msg(uint32_t bridge_if_num, uint8_t *addr);
+nss_tx_status_t nss_bridge_tx_set_mac_addr_msg(uint32_t bridge_if_num, const uint8_t *addr);
 
 /**
  * nss_bridge_tx_join_msg
--- a/exports/nss_edma.h
+++ b/exports/nss_edma.h
@@ -327,6 +327,15 @@ extern struct nss_ctx_instance *nss_edma
 extern void nss_edma_notify_unregister(void);
 
 /**
+ * nss_edma_get_context
+ *	Gets the EDMA NSS context.
+ *
+ * @return
+ * Pointer to the EDMA NSS core context.
+ */
+extern struct nss_ctx_instance *nss_edma_get_context(void);
+
+/**
  * nss_edma_get_stats
  *	Sends EDMA statistics to NSS clients.
  *
--- a/exports/nss_freq.h
+++ b/exports/nss_freq.h
@@ -45,6 +45,15 @@
 nss_tx_status_t nss_freq_change(struct nss_ctx_instance *nss_ctx, uint32_t eng, uint32_t stats_enable, uint32_t start_or_end);
 
 /**
+ * nss_freq_get_context
+ *	Gets the NSS frequency context.
+ *
+ * @return
+ * Pointer to the NSS frequency context.
+ */
+extern struct nss_ctx_instance *nss_freq_get_context(void);
+
+/**
  * nss_freq_get_cpu_usage
  *	Returns the CPU usage value in percentage at any instance for a required core. Range of usage is 0-100.
  *
--- a/exports/nss_gre_tunnel.h
+++ b/exports/nss_gre_tunnel.h
@@ -212,7 +212,11 @@ struct nss_gre_tunnel_stats {
  *	GRE tunnel transmission statistics structure.
  */
 struct nss_gre_tunnel_stats_notification {
+#if defined(NSS_HAL_IPQ807x_SUPPORT)
 	uint64_t stats_ctx[NSS_GRE_TUNNEL_STATS_SESSION_MAX + NSS_CRYPTO_CMN_RESP_ERROR_MAX];
+#else
+	uint64_t stats_ctx[NSS_GRE_TUNNEL_STATS_SESSION_MAX];
+#endif
 							/**< Context transmission statistics. */
 	uint32_t core_id;				/**< Core ID. */
 	uint32_t if_num;				/**< Interface number. */
@@ -338,6 +342,17 @@ extern void nss_gre_tunnel_msg_init(stru
 extern struct nss_ctx_instance *nss_gre_tunnel_get_ctx(void);
 
 /**
+ * nss_gre_tunnel_ifnum_with_core_id()
+ *	Appends core ID to a GRE tunnel interface number.
+ *
+ * @param[in] if_num  NSS interface number.
+ *
+ * @return
+ * Interface number with core ID appended.
+ */
+extern int nss_gre_tunnel_ifnum_with_core_id(int if_num);
+
+/**
  * nss_gre_tunnel_register_if
  *	Registers a network device with the NSS for sending and receiving tunnel
  *	messages.
--- a/exports/nss_if.h
+++ b/exports/nss_if.h
@@ -304,6 +304,17 @@ extern struct nss_ctx_instance *nss_if_r
 					struct net_device *if_ctx);
 
 /**
+ * nss_if_unregister
+ *	Unregisters the NSS interface callback handlers.
+ *
+ * @param[in] if_num  NSS interface number.
+ *
+ * @return
+ * None.
+ */
+extern void nss_if_unregister(uint32_t if_num);
+
+/**
  * nss_if_tx_buf
  *	Sends GMAC packets to a specific physical or virtual network interface.
  *
--- a/exports/nss_ipv4.h
+++ b/exports/nss_ipv4.h
@@ -290,7 +290,9 @@ enum nss_ipv4_stats_types {
 #define NSS_IPV4_RULE_CREATE_IDENTIFIER_VALID 0x1000
 		/**< Identifier is valid. */
 #define NSS_IPV4_RULE_CREATE_MIRROR_VALID 0x2000	/**< Mirror fields are valid. */
+#ifdef NSS_FIRMWARE_VERSION_12_5
 #define NSS_IPV4_RULE_CREATE_RAWIP_VALID 0x4000		/**< RAW IP fields are valid. */
+#endif
 
 /*
  * Multicast command rule flags
@@ -1052,6 +1054,18 @@ int nss_ipv4_max_conn_count(void);
 typedef void (*nss_ipv4_msg_callback_t)(void *app_data, struct nss_ipv4_msg *msg);
 
 /**
+ * nss_ipv4_conn_inquiry
+ *	Inquires whether a connection has been established in NSS firmware.
+ *
+ * @param[in] ipv4_5t_p  Pointer to the IPv4 5-tuple.
+ * @param[in] cb         Callback function for the message.
+ *
+ * @return
+ * Status of the Tx operation.
+ */
+extern nss_tx_status_t nss_ipv4_conn_inquiry(struct nss_ipv4_5tuple *ipv4_5t_p, nss_ipv4_msg_callback_t cb);
+
+/**
  * nss_ipv4_tx
  *	Transmits an IPv4 message to the NSS.
  *
--- a/exports/nss_ipv4_reasm.h
+++ b/exports/nss_ipv4_reasm.h
@@ -53,6 +53,24 @@ struct nss_ipv4_reasm_stats_notification
 #ifdef __KERNEL__ /* only kernel will use. */
 
 /**
+ * nss_ipv4_reasm_get_context
+ *	Gets the NSS context instance for IPv4 reassembly.
+ *
+ * @return
+ * NSS context instance for IPv4 reassembly.
+ */
+extern struct nss_ctx_instance *nss_ipv4_reasm_get_context(void);
+
+/**
+ * nss_ipv4_reasm_register_handler
+ *	Registers the IPv4 reassembly message handler.
+ *
+ * @return
+ * None.
+ */
+extern void nss_ipv4_reasm_register_handler(void);
+
+/**
  * nss_ipv4_reasm_stats_register_notifier
  *	Registers a statistics notifier.
  *
--- a/exports/nss_ipv6.h
+++ b/exports/nss_ipv6.h
@@ -1047,6 +1047,22 @@ extern int nss_ipv6_max_conn_count(void)
 typedef void (*nss_ipv6_msg_callback_t)(void *app_data, struct nss_ipv6_msg *msg);
 
 /**
+ * nss_ipv6_conn_inquiry
+ *	Inquires whether an IPv6 connection has been established in NSS firmware.
+ *
+ * @datatypes
+ * nss_ipv6_5tuple \n
+ * nss_ipv6_msg_callback_t
+ *
+ * @param[in] ipv6_5t_p  Pointer to the IPv6 5-tuple.
+ * @param[in] cb         Callback function for the message response.
+ *
+ * @return
+ * Status of the Tx operation.
+ */
+extern nss_tx_status_t nss_ipv6_conn_inquiry(struct nss_ipv6_5tuple *ipv6_5t_p, nss_ipv6_msg_callback_t cb);
+
+/**
  * nss_ipv6_tx
  *	Transmits an IPv6 message to the NSS.
  *
--- a/exports/nss_ipv6_reasm.h
+++ b/exports/nss_ipv6_reasm.h
@@ -57,6 +57,21 @@ struct nss_ipv6_reasm_stats_notification
 #ifdef __KERNEL__
 
 /**
+ * nss_ipv6_reasm_get_context
+ *	Gets the NSS context instance for IPv6 reassembly.
+ *
+ * @return
+ * NSS context instance for IPv6 reassembly.
+ */
+extern struct nss_ctx_instance *nss_ipv6_reasm_get_context(void);
+
+/**
+ * nss_ipv6_reasm_register_handler
+ *	Registers the IPv6 reassembly message handler.
+ */
+extern void nss_ipv6_reasm_register_handler(void);
+
+/**
  * nss_ipv6_reasm_stats_register_notifier
  *	Registers a statistics notifier.
  *
--- a/exports/nss_match.h
+++ b/exports/nss_match.h
@@ -247,7 +247,7 @@ extern struct nss_ctx_instance *nss_matc
  * @return
  * Pointer to the NSS core context.
  */
-extern struct nss_ctx_instance *nss_match_register_instance(int if_num, nss_match_msg_sync_callback_t notify_cb);
+extern struct nss_ctx_instance *nss_match_register_instance(int if_num, void (*notify_cb)(void *, struct nss_cmn_msg *));
 
 /**
  * nss_match_unregister_instance
--- a/exports/nss_n2h.h
+++ b/exports/nss_n2h.h
@@ -443,6 +443,23 @@ struct nss_n2h_msg {
 typedef void (*nss_n2h_msg_callback_t)(void *app_data, struct nss_n2h_msg *msg);
 
 /**
+ * nss_n2h_notify_register
+ *	Registers to receive N2H events.
+ *
+ * @datatypes
+ * nss_ctx_instance \n
+ * nss_n2h_msg_callback_t
+ *
+ * @param[in] core      NSS core number.
+ * @param[in] cb        Callback function for N2H notifications.
+ * @param[in] app_data  Pointer to the application context of the message.
+ *
+ * @return
+ * Pointer to the NSS core context, or NULL on failure.
+ */
+extern struct nss_ctx_instance *nss_n2h_notify_register(int core, nss_n2h_msg_callback_t cb, void *app_data);
+
+/**
  * nss_n2h_tx_msg
  *	Sends messages to the N2H package.
  *
--- a/exports/nss_sjack.h
+++ b/exports/nss_sjack.h
@@ -134,6 +134,14 @@ extern struct nss_ctx_instance *nss_sjac
 extern void nss_sjack_unregister_if(uint32_t if_num);
 
 /**
+ * nss_sjack_get_context()
+ *	Gets the NSS context instance for SJACK.
+ *
+ * @return NSS context instance for SJACK.
+ */
+extern struct nss_ctx_instance *nss_sjack_get_context(void);
+
+/**
  * nss_sjack_tx_msg
  *	Send SJACK messages to the NSS.
  *
--- a/exports/nss_vlan.h
+++ b/exports/nss_vlan.h
@@ -210,7 +210,7 @@ nss_tx_status_t nss_vlan_tx_set_mtu_msg(
  * @return
  * Status of the Tx operation.
  */
-nss_tx_status_t nss_vlan_tx_set_mac_addr_msg(uint32_t vlan_if_num, uint8_t *addr);
+nss_tx_status_t nss_vlan_tx_set_mac_addr_msg(uint32_t vlan_if_num, const uint8_t *addr);
 
 /**
  * nss_vlan_tx_vsi_attach_msg
--- a/exports/nss_vxlan.h
+++ b/exports/nss_vxlan.h
@@ -343,6 +343,17 @@ extern void nss_vxlan_msg_init(struct ns
 				nss_vxlan_msg_callback_t cb, void *app_data);
 
 /**
+ * nss_vxlan_ifnum_with_core_id()
+ *	Appends core ID to a VxLAN interface number.
+ *
+ * @param[in] if_num  NSS interface number.
+ *
+ * @return
+ * Interface number with core ID appended.
+ */
+extern int nss_vxlan_ifnum_with_core_id(int if_num);
+
+/**
  * nss_vxlan_get_ctx()
  *	Get VxLAN context.
  *
--- a/exports/nss_wifi_vdev.h
+++ b/exports/nss_wifi_vdev.h
@@ -1404,7 +1404,7 @@ nss_tx_status_t nss_wifi_vdev_base_set_n
  * @return
  * Status of the Tx operation.
  */
-nss_tx_status_t nss_wifi_vdev_set_peer_next_hop(struct nss_ctx_instance *nss_ctx, uint32_t nss_if, uint8_t *addr, uint32_t next_hop_if);
+nss_tx_status_t nss_wifi_vdev_set_peer_next_hop(struct nss_ctx_instance *nss_ctx, uint32_t nss_if, const uint8_t *addr, uint32_t next_hop_if);
 
 /*
  * nss_wifi_vdev_set_dp_type
--- a/exports/nss_wifili_if.h
+++ b/exports/nss_wifili_if.h
@@ -751,6 +751,15 @@ struct nss_wifili_stats_notification {
 	struct nss_wifili_stats stats;	/**< Wifili statistics. */
 };
 
+/**
+ * nss_wifili_get_context
+ *	Gets the Wi-Fi Li context used in NSS GRE transmit.
+ *
+ * @return
+ * Pointer to the NSS core context.
+ */
+extern struct nss_ctx_instance *nss_wifili_get_context(void);
+
 #ifdef __KERNEL__ /* only kernel will use. */
 
 /**
@@ -1487,8 +1496,10 @@ struct nss_wifili_rx_ctrl_stats {
 	uint32_t bcast_rcv_bytes;		/**< Total number of broadcast bytes received. */
 	uint32_t defrag_mcast_drop;		/**< Total number of defrag multicast dropped packets. */
 	uint32_t mcast_3addr_drop;		/**< Total number of 3 address multicast dropped packets. */
+#ifdef NSS_FIRMWARE_VERSION_12_5
 	uint32_t ucast_rcv_cnt;			/**< Total number of unicast packets received. */
 	uint32_t ucast_rcv_bytes;		/**< Total number of unicast bytes received. */
+#endif
 };
 
 /**
@@ -1500,8 +1511,10 @@ struct nss_wifili_retry_ctrl_stats {
 	uint32_t tx_retry_count;		/**< Transmit retry count. */
 	uint32_t tx_multiple_retry_count;	/**< Transmit multiple retry count. */
 	uint32_t rx_retry_count;		/**< Receive retry count. */
+#ifdef NSS_FIRMWARE_VERSION_12_5
 	uint32_t tx_mpdu_retry_count;		/**< Per mpdu retry count. */
 	uint32_t tx_mpdu_total_retry_count;	/**< Total mpdu retry count. */
+#endif
 };
 
 /**
@@ -2126,6 +2139,25 @@ extern nss_tx_status_t nss_wifili_tx_msg
 extern nss_tx_status_t nss_wifili_tx_msg_sync(struct nss_ctx_instance *nss_ctx, struct nss_wifili_msg *msg);
 
 /**
+ * nss_wifili_msg_init
+ *	Initialize a wifili message.
+ *
+ * @datatypes
+ * nss_wifili_msg
+ *
+ * @param[in,out] ncm       Pointer to the Wi-Fi message.
+ * @param[in]     if_num    NSS interface number.
+ * @param[in]     type      Type of message.
+ * @param[in]     len       Size of the message.
+ * @param[in]     cb        Message callback.
+ * @param[in]     app_data  Application context of the message.
+ *
+ * @return
+ * void
+ */
+extern void nss_wifili_msg_init(struct nss_wifili_msg *ncm, uint16_t if_num, uint32_t type, uint32_t len, void *cb, void *app_data);
+
+/**
  * nss_wifili_msg_callback_t
  *	Callback to receive wifili messages.
  *
@@ -2263,7 +2295,7 @@ void nss_wifili_release_external_if(nss_
  */
 uint8_t nss_wifili_thread_scheme_alloc(struct nss_ctx_instance *nss_ctx,
 				int32_t radio_ifnum,
-				uint32_t radio_priority);
+				enum nss_wifili_thread_scheme_priority radio_priority);
 
 /**
  * nss_wifili_thread_scheme_dealloc
