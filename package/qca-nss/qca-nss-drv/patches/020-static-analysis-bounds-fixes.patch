--- a/nss_capwap.c
+++ b/nss_capwap.c
@@ -252,6 +252,11 @@ static void nss_capwap_msg_handler(struc
 	case NSS_CAPWAP_MSG_TYPE_SYNC_STATS: {
 			uint32_t if_num;
 
+			if (!nss_capwap_verify_if_num(ncm->interface)) {
+				nss_warning("%px: received invalid interface %d for CAPWAP stats", nss_ctx, ncm->interface);
+				return;
+			}
+
 			if_num = ncm->interface - NSS_DYNAMIC_IF_START;
 			if (nss_capwap_hdl[if_num] != NULL) {
 				/*
--- a/nss_ipv4.c
+++ b/nss_ipv4.c
@@ -449,7 +449,7 @@ static int nss_ipv4_conn_cfg_process(str
 
 	nss_ipv4_ct_info.cme_mem = __get_free_pages(GFP_ATOMIC | __GFP_NOWARN | __GFP_ZERO,
 					get_order(nss_ipv4_ct_info.cme_table_size));
-	if (!nss_ipv4_ct_info.ce_mem) {
+	if (!nss_ipv4_ct_info.cme_mem) {
 		nss_warning("%px: Memory allocation failed for IPv4 Connections: %d\n",
 							nss_ctx,
 							conn);
--- a/nss_ipv6_stats.c
+++ b/nss_ipv6_stats.c
@@ -55,8 +55,10 @@ static ssize_t nss_ipv6_stats_read(struc
 	}
 
 	/*
-	 * Note: The assumption here is that exception event count is larger than other statistics count for IPv6.
+	 * Allocate enough room for both normal IPv6 stats and exception stats.
+	 * The current firmware ABI keeps the exception table as the larger one.
 	 */
+	BUILD_BUG_ON((int)NSS_IPV6_STATS_MAX > (int)NSS_IPV6_EXCEPTION_EVENT_MAX);
 	stats_shadow = kzalloc(NSS_IPV6_EXCEPTION_EVENT_MAX * 8, GFP_KERNEL);
 	if (unlikely(stats_shadow == NULL)) {
 		nss_warning("Could not allocate memory for local shadow buffer");
--- a/nss_ppe_vp_stats.c
+++ b/nss_ppe_vp_stats.c
@@ -91,9 +91,18 @@ struct nss_stats_info nss_ppe_vp_stats_s
  */
 void nss_ppe_vp_stats_sync(struct nss_ctx_instance *nss_ctx, struct nss_ppe_vp_sync_stats_msg *stats_msg, uint16_t if_num)
 {
-	uint16_t count = stats_msg->count;
+	uint16_t count;
 	uint16_t vp_index, i;
 
+	/*
+	 * Validate count before indexing the fixed-size VP stats array.
+	 */
+	if (stats_msg->count > NSS_PPE_VP_MAX_NUM) {
+		nss_warning("%px: Invalid count %d exceeds maximum %d\n", nss_ctx, stats_msg->count, NSS_PPE_VP_MAX_NUM);
+		return;
+	}
+
+	count = stats_msg->count;
 	spin_lock_bh(&nss_ppe_vp_stats_lock);
 
 	/*
@@ -110,9 +119,14 @@ void nss_ppe_vp_stats_sync(struct nss_ct
 		count--;
 
 		/*
-		 * Update stats in global array
+		 * Calculate and validate the VP index before updating stats.
 		 */
 		vp_index = stats_msg->vp_stats[count].ppe_port_num - NSS_PPE_VP_START;
+		if (vp_index >= NSS_PPE_VP_MAX_NUM) {
+			nss_warning("%px: Invalid vp_index %d from ppe_port_num %d\n",
+					nss_ctx, vp_index, stats_msg->vp_stats[count].ppe_port_num);
+			continue;
+		}
 		nss_ppe_vp_debug_stats.vp_stats[vp_index].ppe_port_num = stats_msg->vp_stats[count].ppe_port_num;
 		nss_ppe_vp_debug_stats.vp_stats[vp_index].nss_if = stats_msg->vp_stats[count].nss_if;
 		nss_ppe_vp_debug_stats.vp_stats[vp_index].rx_packets += stats_msg->vp_stats[count].stats.rx_packets;
